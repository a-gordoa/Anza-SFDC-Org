public with sharing class StaticPlaylistContainer {
    
    public static List<Playlist__c> playlistContainerList = new List<Playlist__c>();

    playlistHolder static StaticPlaylistContainer(Playlist__c playlist) {
        
        
    }
}
